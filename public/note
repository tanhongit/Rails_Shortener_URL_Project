TEST: 
$ rspec

Part 1.

rails g rspec:install 

**run test rand:
$ irb 
> rand
> (rand * 3)
> (rand * 3).round

-------------------------

**run test generate the hash of the string
$ irb 
> Digest::SHA256.hexdigest('hello')

Part 2.

rails g model Link lookup_code:string original_url:string
rails db:migrate
rails db:migrate RAILS_ENV=test

Part 3.

$ rails c
> SecureRandom
> SecureRandom.uuid

Part 6.

rails g controller Home index

check info create link:
    add "binding.pry" to create method of link controller
    run $ rails s
    add new link for form and click submit
    go back terminal at rails s, press enter 
    > @link
    > @link.persisted?
    > @link.errors

Part 7. 

$ irb 
> URI
> URI.parse("https://www.google.com")
> uri = _
> uri.domain
> uri.public_methods(false).sort
> uri.host
> URI.parse "resgregergre"

Part 10.

rails g devise:install
rails g devise User
rails g devise:views
--------------------------------------------------------
https://www.youtube.com/channel/UCaT8uPGUYbdRsjm4cJy1D7w